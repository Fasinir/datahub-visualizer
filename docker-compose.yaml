services:
  frontend:
    build: frontend
    ports:
      - "4200:80"
    networks:
      - front-tier
      - back-tier
  
  backend:
    build: backend
    ports:
      - "8080:8080"
    networks:
      - back-tier

networks:
  front-tier: {}
  back-tier: {}